model List<Loopify.Models.Negocio>

@{
    Layout = "~/Views/Shared/ClienteLayout.cshtml";
}

<div id="carouselExampleIndicators" class="carousel slide mb-4" data-bs-ride="carousel">
    <div class="carousel-indicators">
        <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
        <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="1" aria-label="Slide 2"></button>
    </div>
    <div class="carousel-inner">
        <div class="carousel-item active">
            <img src="~/Assets/banner.jpg" class="d-block w-100" alt="Banner 1" style="height: 400px; object-fit: cover;">
        </div>
        <div class="carousel-item">
            <img src="~/Assets/banner.jpg" class="d-block w-100" alt="Banner 2" style="height: 400px; object-fit: cover;">
        </div>
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>
</div>

<div class="container-fluid">
    <h2 class="text-center mb-4" style="color: #00A79D;">Promociones del Mes</h2>
    <div class="row">
        @if (Model != null && Model.Any())
        {
            foreach (var negocio in Model)
            {
                <div class="col-12 mb-4">
                    <h4 class="mb-3" style="color: #333; font-weight: bold;">@negocio.Nombre</h4>
                    <div class="row">
                        @foreach (var producto in negocio.Productos)
                        {
                            <div class="col-md-3 mb-4">
                                <div class="card shadow-sm" style="border-radius: 15px;">
                                    <img src="@Url.Content($"~/Assets/{producto.Imagen}")" class="card-img-top" alt="@producto.Nombre" style="height: 200px; object-fit: cover;">
                                    <div class="card-body text-center">
                                        <h5 class="card-title" style="color: #00A79D;">@producto.Nombre</h5>
                                        <p class="card-text">
                                            Precio Original: <s>S/@producto.PrecioOriginal</s><br />
                                            Precio con Descuento: <strong>S/@producto.PrecioDescuento</strong>
                                        </p>
                                        <button class="btn btn-success btn-sm">Agregar</button>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            }
        }
        else
        {
            <p class="text-center">No hay promociones disponibles en este momento.</p>
        }
    </div>
</div>





@*@model List<Loopify.Models.Negocio>

    @{
        Layout = "~/Views/Shared/ClienteLayout.cshtml";
    }

    <div class="container mt-4">
        <h2 class="text-center mb-4" style="color: #00A79D; font-family: 'Inter', sans-serif;">Promociones del Mes</h2>
        <div class="row">
            @if (Model != null && Model.Any())
            {
                foreach (var negocio in Model)
                {
                    <div class="col-12 mb-4">
                        <h4 class="mb-3" style="color: #333; font-weight: bold;">@negocio.Nombre</h4>
                        <div class="row">
                            @foreach (var producto in negocio.Productos)
                            {
                                <div class="col-md-3 mb-4">
                                    <div class="card shadow-sm" style="border-radius: 15px; overflow: hidden;">
                                        <div class="image-container" style="height: 200px; background-color: #f8f9fa;">
                                            <img src="@Url.Content($"~/Assets/{producto.Imagen}")" class="card-img-top h-100 w-100" style="object-fit: cover;" alt="@producto.Nombre">
                                        </div>
                                        <div class="card-body text-center">
                                            <h5 class="card-title" style="color: #00A79D; font-family: 'Inter', sans-serif;">@producto.Nombre</h5>
                                            <p class="card-text" style="color: #555; font-size: 14px;">
                                                Precio Original: <s>S/@producto.PrecioOriginal</s><br />
                                                Precio con Descuento: <strong style="color: #00A79D;">S/@producto.PrecioDescuento</strong>
                                            </p>
                                            <button class="btn btn-success btn-sm">Agregar</button>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                }
            }
            else
            {
                <div class="col-12">
                    <p class="text-center" style="color: #555;">No hay negocios disponibles.</p>
                </div>
            }
        </div>
    </div>*@
